const mytodos=[]
mytodos.push('Buy Bread')
mytodos.push('go for gym')
mytodos.push('upload youtube videos')

// mytodos.forEach(function(todo,index)
// {
//     console.log(`your task no is${index+1}and task is ${todo}`)
// })

    
    let say = function(m,n)
    {
        console.log(`your task no is${index+1}and task is ${todo}`)
    }
   
    say(mytodos.forEach(todo,index))

        
